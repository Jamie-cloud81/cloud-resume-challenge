name: Deploy to S3

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AKIAXNDP37G7PP6M63XN }}
          aws-secret-access-key: ${{ secrets.RwHQ5721eN+hoMIBdIlpsGCUWgV053MlqKnZGoEx }}
          aws-region: ${{ secrets.us-east-2 }}

      - name: Sync site to S3
        run: |
          aws s3 sync . s3://${{ secrets.S3_BUCKET }} \
            --delete \
            --exclude ".git/*" \
            --exclude ".github/*"

      - name: Invalidate CloudFront cache (optional)
        run: echo "We’ll add this once we store DIST_ID as a secret"
